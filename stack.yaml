name: default

service: 
- name: web
  resources:
  - ref: redis/web-redis
  - ref: cloudsql/web-sql
 
router:
- name: web-router
  domain: livedemo.jmahood.com
  default-route:
    ref: service/web

redis:
- name: web-redis
  instance: 
    memory-size-gb: 4

  
  
